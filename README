Dependencies:

git clone git://github.com/robertmeta/win32-memcache.git
cd .\win32-memcache
git submodule init
git submodule update

Then just build libevent followed by memcached.

The memcached website is at:

    http://www.danga.com/memcached/

